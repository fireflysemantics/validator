/**
 * Constant string literal used for message completion
 * when property values are stored in arrays.
 */
export const PREFIX_EACH:string = "Each value in";

/**
 * Constant string literal used for message completion
 * when property values are stored in arrays.
 */
export const PREFIX_SINGLE:string = "The value contained by";