import { ValidationConstraintEnum } from "@fireflysemantics/is/lib/ValidationConstraintEnum";
import { Types } from "@fireflysemantics/is/lib/types";
/**
 * @param message The error message
 * @param value The value that violates the constraint
 * @param field The name of the field that triggered the constraint violation
 * @param constraint The name of the constraint violated
 * @param type The expected type for the field
 * @param code The application or module code for the error
 */
export declare class IsError extends Error {
    message: string;
    value: any;
    field?: string;
    constraint?: ValidationConstraintEnum;
    type?: Types;
    code?: string;
    constructor(message: string, value: any, field?: string, constraint?: ValidationConstraintEnum, type?: Types, code?: string);
}
