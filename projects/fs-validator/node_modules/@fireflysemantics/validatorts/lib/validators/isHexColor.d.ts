/**
 * Checks whether the `target` is hexcolor
 *
 * @param target The target string
 * @return true if the `target` is hexcolor, false otherwise
 */
export declare function isHexColor(str: any): boolean;
