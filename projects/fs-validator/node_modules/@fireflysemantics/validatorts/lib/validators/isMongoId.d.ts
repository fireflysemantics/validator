/**
 * Checks whether the `target` string is a valid MongoID
 *
 * @param target The target string
 * @return true if the `target` is a valid MongoID, false otherwise
 */
export declare function isMongoId(target: string): boolean;
