/**
 * IsEmail Options.
 */
export interface IsEmailOptions {
    require_display_name?: boolean;
    allow_display_name?: boolean;
    allow_utf8_local_part?: boolean;
    require_tld?: boolean;
}
export declare const default_email_options: IsEmailOptions;
/**
 * Checks whether the `target` string is a valid email address
 *
 * @param target The target string
 * @param options The options
 * @return true if the `target` is a valid email address, false otherwise
 */
export declare function isEmail(str: string, options: any): boolean;
