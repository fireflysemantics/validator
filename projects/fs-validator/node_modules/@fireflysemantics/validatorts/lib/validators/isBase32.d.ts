/**
 * Checks whether the `target` is base32 encoded
 *
 * @param target The target string
 * @return true if the `target` is base32 encoded, false otherwise
 */
export declare function isBase32(str: string): boolean;
