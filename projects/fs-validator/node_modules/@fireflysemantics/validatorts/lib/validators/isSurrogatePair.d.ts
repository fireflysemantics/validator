/**
 * Check if `target` is a valid surrogate pair
 *
 * @param target The target
 * @return true if the `target` a valid surrogate pair, false otherwise
 */
export declare function isSurrogatePair(target: string): boolean;
