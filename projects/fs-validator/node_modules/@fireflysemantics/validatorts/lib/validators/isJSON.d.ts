/**
 * Checks whether the `target` string is valid JSON
 *
 * @param target The target string
 * @return true if the `target` is a valid ISSN, false otherwise
 */
export declare function isJSON(target: string): boolean;
