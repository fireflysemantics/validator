/**
 * Test whether the target string is an ISBN number.
 *
 * @param target The string
 * @return true if the `target` string is an ISIN number, false otherwise
 */
export declare function isISIN(target: string): boolean;
