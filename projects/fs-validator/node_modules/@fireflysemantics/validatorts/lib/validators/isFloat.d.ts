/**
 * Checks whether the `target` string is a float
 *
 * @param target The target string
 * @param options The options
 * @return true if the `target` is is a float, false otherwise
 */
export declare function isFloat(str: string, options?: any): boolean;
export declare const floatLocales: string[];
