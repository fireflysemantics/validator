/**
 * Checks whether the `target` is hexadecimal
 *
 * @param target The target string
 * @return true if the `target` is hexadecimal, false otherwise
 */
export declare function isHexadecimal(str: string): boolean;
