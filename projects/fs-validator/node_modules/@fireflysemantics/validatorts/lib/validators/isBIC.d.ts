/**
 * Checks whether the `target` is BIC encoded
 *
 * @param target The target string
 * @return true if the `target` is BIC encoded, false otherwise
 */
export declare function isBIC(str: string): boolean;
