/**
 * Checks whether the `target` string is a valid locale
 *
 * @param target The target string
 * @return true if the `target` is a valid locale, false otherwise
 */
export declare function isLocale(target: string): boolean;
