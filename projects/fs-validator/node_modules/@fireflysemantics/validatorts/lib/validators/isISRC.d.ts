/**
 * Checks whether the `target` string is a valid ISRC
 *
 * @param target The target string
 * @return true if the `target` is a valid ISRC, false otherwise
 */
export declare function isISRC(target: string): boolean;
