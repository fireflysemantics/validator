/**
 * Checks whether the `target` string is a valid LatLong
 *
 * @param target The target string
 * @return true if the `target` is a valid LatLong, false otherwise
 */
export declare function isLatLong(target: string): boolean;
